package pages.os;

import io.appium.java_client.android.AndroidDriver;
import pages.BasePage;

public class SensorsPage extends BasePage {
    public SensorsPage(AndroidDriver driver) {
        super(driver);
    }
}
