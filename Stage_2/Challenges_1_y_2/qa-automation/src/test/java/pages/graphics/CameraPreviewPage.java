package pages.graphics;

import io.appium.java_client.android.AndroidDriver;
import pages.BasePage;

public class CameraPreviewPage extends BasePage {
    public CameraPreviewPage(AndroidDriver driver) {
        super(driver);
    }
}
