package pages.graphics;

import io.appium.java_client.android.AndroidDriver;
import pages.BasePage;

public class CreateBitmapPage extends BasePage {
    public CreateBitmapPage(AndroidDriver driver) {
        super(driver);
    }
}
