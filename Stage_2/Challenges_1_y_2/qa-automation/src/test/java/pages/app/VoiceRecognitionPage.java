package pages.app;

import io.appium.java_client.android.AndroidDriver;
import pages.BasePage;

public class VoiceRecognitionPage extends BasePage {
    public VoiceRecognitionPage(AndroidDriver driver) {
        super(driver);
    }
}
